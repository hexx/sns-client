# 引き継ぎプロンプト: 「スレッド／会話表示」仕様グリリングの続き（Q2 から）

前セッションの続きをお願いします。まず以下を**この順で**読んでください:

1. `/workspace/.handoff/STATE.md` — セッション状態（最重要。前セッションの失敗と教訓・確定事実・確定済み決定・仮ドラフトの扱い）
2. `/home/pi/.pi/agent/skills/grill-with-docs/SKILL.md` — スキル定義（/grilling を実行し /domain-modeling を使う）
3. `/home/pi/.pi/agent/skills/grilling/SKILL.md` と `/home/pi/.pi/agent/skills/domain-modeling/SKILL.md`（および同ディレクトリの CONTEXT-FORMAT.md / ADR-FORMAT.md）
4. `/workspace/CONTEXT.md` — ドメイン語彙集
5. `/workspace/docs/thread-view-spec.md` — 前 AI が書いた**仮ドラフト**（未承認の推奨を含む。確定決定で上書きする対象）

## タスク
改善テーマ「スレッド／会話表示」の仕様を /grill-with-docs でまとめる。**Q1（方向性＝スレッド表示）のみ確定済み。Q2〜Q9 は未確定**で、Q2 からやり直す（ユーザーの明示指示）。

## 絶対の注意事項（前セッションの失敗の再発防止）
- **「あなたの推奨どおりで良いです」はそのとき問うた1問のみの承認**。残りの全質問への白紙委任ではない。必ず1問ずつ質問し、回答を待つ。複数問を一度に聞かない。
- 先走り禁止。Q2〜Q9 を自分の推奨で「確定」とみなしてドキュメントを確定させない。仮ドラフト（thread-view-spec.md / ADR-0017 / CONTEXT.md）は推奨ベースなので、**ユーザーの確定回答を得た箇所から順に上書き**する。
- 合意前に実装しない。compaction の自動継続通知を実装のゴーサインと解釈しない（前 AI はこれで空回りした）。

## 再開手順
1. **Q2（スレッド画面の開き方: A=オーバーレイ／B=ルーター導入／C=デッキカラム、推奨 A）を提示済み・回答待ち**。まず Q2 の回答を受ける。
2. 続いて Q3（スコープと名前）→ Q4（データ取得）→ … と、必要な枝を**1問ずつ**尋ねる。必ず推奨を添える。事実は質問せずコードベースで調べる。
3. 用語が確定したら CONTEXT.md をその場で更新（仮ドラフトの Thread/reply 項を確定内容で上書き）。
4. 全決定後、共通理解をユーザーに確認し、承諾後に thread-view-spec.md を確定版へ整備。ADR は3条件（覆しにくい／文脈なしでは意外／本物のトレードオフ）を満たす場合のみ（0017 が相当するか再評価）。

## やり取りの制約（厳守）
- ユーザーへの回答・質問はすべて日本語。
- すべての返答は「ファイルに書く → 日本語を確認する → ファイルを読み込む → 読み込んだ内容を**そのまま**返す」手順で行う。言い換え・書き直し・翻訳（とくに英語化）は禁止。前セッションは繰り返しドリフトしてユーザーを怒らせた。
